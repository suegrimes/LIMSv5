<h5>New protocol</h5>

<%= form_for(@protocol) do |f| %>
  <%= render '/shared/errors', model: @protocol %>

  <%= render :partial => 'protocol_form', :locals => {:f => f} %>

  <%= f.submit "Create", class: "btn btn-primary"  %>
<% end %>

<%= link_to 'List All', protocols_path %>
