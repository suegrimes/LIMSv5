<% @nr_libs = @new_lib.size %>
jQuery('#add_libs').hide();
jQuery('#splex_libs').html("<%= escape_javascript(render :partial => 'sample_form').html_safe %>");
jQuery('#add_libs_button').css({
   position : 'relative',
   top : '42px',
   left : '125px'
});
jQuery('#lib_button').prop('value', 'Refresh from Defaults');
